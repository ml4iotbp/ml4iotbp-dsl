dataset:
  churn_ds:
    train-ratio: 0.6
    val-ratio: 0.3
    test-ratio: 0.3

models:
  churn_model:
    type: Classification
    algorithm: Random Forest
    dataset: churn_ds
    training:
      type: Batch
      metrics: [accuracy]
    hyperparameters:
      n_estimators: 10